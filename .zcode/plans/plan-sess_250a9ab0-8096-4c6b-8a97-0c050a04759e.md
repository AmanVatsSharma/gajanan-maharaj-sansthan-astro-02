## Goal
Make the contact phone number a true single source of truth, and replace the broken two-state `whatsappBookingOnly` boolean with a single, typed, three-state switch (`both` | `call_only` | `whatsapp_only`) that every contact surface on the site honors — including the ones that currently ignore it.

## Part 1 — Single source of truth for the MODE (the switch)

### 1.1 `src/data/contact.ts` — replace boolean with typed switch
- Remove `whatsappBookingOnly: boolean`.
- Add `export type ContactMode = "both" | "call_only" | "whatsapp_only"`.
- Add `booking.contactMode: "both" as ContactMode` (LIVE default = `both`, per your choice) with an explanatory comment.
- Export two derived flags (the "control" every surface reads) so branching logic lives in exactly one place:
  ```ts
  export const showWhatsAppButton  = CONTACT_DETAILS.booking.contactMode !== "call_only";
  export const callButtonIsDialog  = CONTACT_DETAILS.booking.contactMode === "whatsapp_only";
  ```
- Keep `mobile`, `whatsapp`, `WHATSAPP_LINK` unchanged (these are already the canonical number `+919599417591`).

### 1.2 Wire all contact surfaces to the switch (read `showWhatsAppButton` + `callButtonIsDialog`)

**Behavior rules (applied uniformly):**
- `both`: Call buttons → `tel:`; WhatsApp buttons → `wa.me`.
- `call_only` (`showWhatsAppButton === false`): hide every WhatsApp CTA.
- `whatsapp_only` (`callButtonIsDialog === true`): Call buttons open the booking-request dialog (where a `<BookingDialog>` is mounted) or fall back to a `→ /booking` link ("Send Booking Request") where one isn't; WhatsApp CTAs stay visible.

**Files to update:**
| File | Change |
|---|---|
| `src/components/layout/Navbar.tsx` | Replace local `isWhatsAppOnly` with the two helpers; wrap each WhatsApp surface (desktop btn, mobile icon, drawer btn) in `showWhatsAppButton && …`. Already has `<BookingDialog>` — keep it. |
| `src/components/layout/Footer.tsx` | Same swap. Note: the large phone-number display at line 27-33 stays a `tel:` link in all modes (it's informational text, not a CTA button) — documented exception. |
| `src/features/info/components/CTABanner.tsx` | Same swap + hide WhatsApp button in `call_only`. Already has dialog. |
| `src/components/ui/whatsapp-button.tsx` (floating, site-wide) | Import helpers. Add a `<BookingDialog>` + state; in `whatsapp_only`, the "Call Now" pill opens the dialog. In `call_only`, hide the WhatsApp pills (mobile + desktop). |
| `src/features/booking/components/BookingCheckoutWidget.tsx` (homepage strip) | Wire helpers; in `whatsapp_only` make "Call now" open a dialog; in `call_only` hide "WhatsApp to book". |
| `src/features/booking/components/BookingLandingForm.tsx` | In `whatsapp_only` (it lives INSIDE the dialog / `/booking`), hide its own Call button (the form IS the alternative to calling). In `call_only`, hide WhatsApp button. |
| `src/features/info/components/EnhancedRoomsSection.tsx` (room modal) | In `whatsapp_only`, Call → `→ /booking` link. In `call_only`, hide the WhatsApp link in the modal. |
| `src/features/locations/components/LocationBookingCtas.tsx` | In `whatsapp_only`, "Call to Book" → `→ /booking` link. In `call_only`, hide WhatsApp button. |

(All `BookingDialog` instances use the existing `src/features/booking/components/BookingDialog.tsx`, which already wraps `BookingLandingForm` — no change needed there.)

## Part 2 — Single source of truth for the NUMBER (kill stale copies)

### 2.1 `src/data/faq.ts` (10 hardcoded `+919599417591` strings)
- Add `import { CONTACT_DETAILS } from "@/data/contact"`.
- Convert each of the 10 answer strings to template literals referencing `${CONTACT_DETAILS.booking.mobile}` so they can never drift again.

### 2.2 The 13 `.astro` page files (33 occurrences of the STALE `+91 80531 90691`)
Files: `trimbakeshwar-bhakt-niwas.astro` (7), `narayan-nagbali-trimbakeshwar.astro` (5), `kumbh-mela-trimbakeshwar.astro` (2), `trimbakeshwar-from-pune.astro` (2), `trimbakeshwar-from-mumbai.astro` (1), `trimbakeshwar-best-time-to-visit.astro` (1), `mahashivratri-trimbakeshwar.astro` (1), `mahashivratri-omkareshwar.astro` (1), `omkareshwar-abhishekam-booking.astro` (3), `omkareshwar-prasadam.astro` (1), `omkareshwar-from-pune.astro` (1), `omkareshwar-darshan-timings.astro` (1), `omkareshwar-aarti-timings.astro` (1), `ganesh-chaturthi-omkareshwar-trimbakeshwar.astro` (1).
- In each file: add `import { CONTACT_DETAILS } from "@/data/contact";` to the frontmatter, then replace each `+91 80531 90691` literal with `{CONTACT_DETAILS.booking.mobile}` (in JSX body) or `${CONTACT_DETAILS.booking.mobile}` (inside frontmatter string literals, e.g. JSON-LD `value:`). This is the true single-source fix, not a fresh hardcoded copy.
- These are static informational mentions (not mode-controlled buttons), so they keep showing the canonical number in all modes.

### 2.3 `.env.example:63`
- Replace the stray `+918786420969` with the canonical `+919599417591` so the documented Google-Business-Profile number matches the site.

### 2.4 `scripts/update-phone.ps1` (the root cause of the drift)
- Fix `$newSpaced` to equal `$newPlain`'s canonical value (`+91 89698 71378`), update both old/new number pairs in the header comment to target `9599417591`, and make it truly idempotent. (Alternatively delete it — I'll fix-and-keep, since it's a useful sweep tool when corrected.)

### 2.5 Docs sync (if present)
- If `CLAUDE.md` documents the old `whatsappBookingOnly` flag, update it to describe `contactMode` and the three modes.

## Part 3 — Verification (run after edits)
1. `npm run lint`
2. `npm run build`
3. Ripgrep audit (will run via direct ripgrep path, since the shell `grep` is broken): confirm **0** remaining occurrences of `80531` and `8786420969` anywhere under `src/`, `content/`, `.env.example`; confirm **0** references to the removed `whatsappBookingOnly` symbol remain in code.
4. Spot-check: toggle `contactMode` to each of the 3 values mentally against the rule table to confirm coverage (no edit needed for this — the helpers guarantee it).

## Out of scope (explicitly)
- `content/blog/**` (per AGENTS.md, off-limits). Audit confirmed it contains **zero** phone literals anyway.
- No env-var drive for `contactMode` (you chose "typed config in contact.ts only").
- No new shared visual component forced onto varied buttons (keeps each surface's styling intact); the shared logic lives in the two exported flags.